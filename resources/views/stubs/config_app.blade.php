return [

    /*
    |--------------------------------------------------------------------------
    | Имя приложения
    |--------------------------------------------------------------------------
    |
    | Это значение — название бота. Используется фреймворком в логах,
    | сообщениях об ошибках и других местах, где требуется отобразить
    | имя приложения.
    |
    */

    'name' => '{{ $botName }}',
    'username' => '{{ $botUsername }}',

    /*
    |--------------------------------------------------------------------------
    | URL приложения
    |--------------------------------------------------------------------------
    |
    | Базовый URL, на котором развёрнут бот. Используется для построения
    | абсолютных ссылок и регистрации webhook у мессенджеров.
    |
    */

    'url' => env('APP_URL', 'http://localhost'),

    /*
    |--------------------------------------------------------------------------
    | Окружение
    |--------------------------------------------------------------------------
    |
    | Текущее окружение приложения: production, staging, local и т.п.
    | Влияет на поведение фреймворка и подключаемых сервисов.
    |
    */

    'environment' => '{{ $environment }}',

    /*
    |--------------------------------------------------------------------------
    | Режим отладки
    |--------------------------------------------------------------------------
    |
    | При включённом режиме в логах и ответах могут содержаться подробные
    | сведения об ошибках. На production оставляйте false.
    |
    */

    'debug' => {{ $debug ? 'true' : 'false' }},

    /*
    |--------------------------------------------------------------------------
    | Хранилище состояния диалогов
    |--------------------------------------------------------------------------
    |
    | Драйвер, в котором фреймворк хранит состояние диалогов между шагами:
    | file, database, redis. Влияет на масштабируемость и сохранение
    | контекста между перезапусками.
    |
    */

    'state_storage' => '{{ $stateStorage }}',

];
