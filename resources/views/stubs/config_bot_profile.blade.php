return [

    /*
    |--------------------------------------------------------------------------
    | Имя бота
    |--------------------------------------------------------------------------
    |
    | Отображаемое имя бота в Telegram. Устанавливается через BotFather
    | и синхронизируется командой php artisan bot:profile-sync.
    |
    */

    'name' => '{{ $name }}',

    /*
    |--------------------------------------------------------------------------
    | Короткое описание (about)
    |--------------------------------------------------------------------------
    |
    | Краткое описание до 120 символов, отображается на странице бота
    | в Telegram до начала диалога. Синхронизируется через bot:profile-sync.
    |
    */

    'short_description' => '{{ $shortDescription }}',

    /*
    |--------------------------------------------------------------------------
    | Описание
    |--------------------------------------------------------------------------
    |
    | Полное описание бота до 512 символов, отображается при открытии
    | нового чата. Синхронизируется через bot:profile-sync.
    |
    */

    'description' => '{{ $description }}',

    /*
    |--------------------------------------------------------------------------
    | Команды меню
    |--------------------------------------------------------------------------
    |
    | Список команд, отображаемых в меню кнопки «/» в интерфейсе Telegram.
    | Каждая запись: command (со слэшем) и description. Синхронизируется
    | через bot:profile-sync.
    |
    */

@if(count($commands) === 0)
    'commands' => [],
@else
    'commands' => [
@foreach($commands as $cmd)
        ['command' => '{{ $cmd['command'] }}', 'description' => '{{ $cmd['description'] }}'],
@endforeach
    ],
@endif

];