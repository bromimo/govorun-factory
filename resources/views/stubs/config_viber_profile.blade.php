return [

    /*
    |--------------------------------------------------------------------------
    | Имя отправителя
    |--------------------------------------------------------------------------
    |
    | Имя, которое Viber показывает получателю в каждом исходящем сообщении
    | бота (поле sender.name). Максимум 28 символов.
    |
    */

    'sender_name' => {!! var_export($senderName, true) !!},

    /*
    |--------------------------------------------------------------------------
    | Аватар отправителя
    |--------------------------------------------------------------------------
    |
    | Публичный URL изображения аватара бота (поле sender.avatar).
    | Формируется автоматически из APP_URL после деплоя. Null — без аватара.
    |
    */

    'sender_avatar' => {!! $avatarExpr !!},

    /*
    |--------------------------------------------------------------------------
    | URI публичного аккаунта
    |--------------------------------------------------------------------------
    |
    | Идентификатор публичного аккаунта на Viber (часть ссылки после
    | chats.viber.com/). Используется для формирования deep-link.
    |
    */

    'public_account_uri' => {!! $publicAccountUri === null ? 'null' : var_export($publicAccountUri, true) !!},

    /*
    |--------------------------------------------------------------------------
    | Типы событий webhook
    |--------------------------------------------------------------------------
    |
    | Список типов событий, на которые Viber будет присылать уведомления.
    | Устанавливается при регистрации webhook через messenger:webhook:install.
    |
    */

    'event_types' => [
@foreach($eventTypes as $eventType)
        '{{ $eventType }}',
@endforeach
    ],

];