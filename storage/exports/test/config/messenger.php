<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Драйвер мессенджера по умолчанию
    |--------------------------------------------------------------------------
    |
    | Драйвер, который используется по умолчанию, если иной не указан явно.
    | Берётся из переменной окружения MESSENGER_DRIVER.
    |
    */

    'default' => env('MESSENGER_DRIVER', 'vk'),

    /*
    |--------------------------------------------------------------------------
    | Активные драйверы
    |--------------------------------------------------------------------------
    |
    | Список включённых драйверов мессенджеров. Фреймворк регистрирует
    | webhook и обрабатывает входящие сообщения для каждого из них.
    |
    */

    'drivers' => [
        env('MESSENGER_DRIVER', 'vk'),
    ],

    /*
    |--------------------------------------------------------------------------
    | ВКонтакте
    |--------------------------------------------------------------------------
    |
    | Параметры подключения к VK Callback API.
    | Токен сообщества, secret и confirmation выдаются в настройках
    | сообщества VK.
    |
    */

    'vk' => [
        'token' => env('VK_BOT_TOKEN', ''),
        'secret' => env('VK_SECRET', null),
        'confirmation' => env('VK_CONFIRMATION', ''),
    ],

    /*
    |--------------------------------------------------------------------------
    | Telegram
    |--------------------------------------------------------------------------
    |
    | Параметры подключения к Telegram Bot API.
    | Токен выдаёт @BotFather, secret — секретный ключ для верификации
    | входящих webhook-запросов.
    |
    */

    'telegram' => [
        'token' => env('TELEGRAM_BOT_TOKEN', ''),
        'secret' => env('TELEGRAM_WEBHOOK_SECRET', null),
    ],

];
